1つめのターミナル：
    docker build -t qiicast_image .
    docker run --name qiicast -it -w /app -p 8000:8000 -p 8501:8501 qiicast_image /bin/bash
    cd backend
    uvicorn app:app --port 8000 --reload

2つめのターミナル：
    docker exec -it qiicast /bin/bash
    cd frontend
    streamlit run src/app.py